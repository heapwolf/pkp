{
  "addresses": prompt("Email Address", addresses),
  "name": prompt("Real Name", name),
  "urls": prompt("Validation URLs", urls),
  "origin": prompt("Origin URL", origin),
  "data": data,
  "algorithm": algorithm
}

